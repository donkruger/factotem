# Dependencies
node_modules/
.npm-cache/
# Build output
dist/

# Local data & auth
store/
data/
logs/

# Groups - only track base structure and specific CLAUDE.md files
groups/*
!groups/main/
!groups/global/
groups/main/*
groups/global/*
!groups/main/CLAUDE.md
!groups/global/CLAUDE.md

# Secrets
*.keys.json
.env

# Historical: the auth-mode marker used to live here until 2026-04-24, when it
# moved to ~/.config/nanoclaw/auth-mode (launchd watcher TCC issue — see
# docs/OPERATIONS.md § Auth Mode). Kept gitignored in case a stale file appears.
.auth-mode

# Temp files
.tmp-*

# OS
.DS_Store

# IDE
.idea/
.vscode/
.cursor/

# Skills system (local per-installation state)
.nanoclaw/

agents-sdk-docs
